# defense-roguelike 작업 계약

이 저장소의 게임 작업은 프로젝트 로컬 `indie-game-workflow`를 사용한다. 현재 저장소에는 게임 엔진이나 제품 규칙이 아직 정의되지 않았으므로 이름만으로 기술 스택, 플랫폼, 화면, 입력, 저장 방식 또는 출시 대상을 추정하지 않는다.

## 작업 시작

1. 모든 새 게임 작업과 재개 작업에서 다른 게임 스킬보다 먼저 [game-workflow](.agents/skills/game-workflow/SKILL.md)를 적용한다.
2. 저장소 또는 제품을 변경하거나, 여러 단계가 필요하거나, 기존 실행을 재개하는 작업은 라우팅 직후 하나의 [game-workflow-supervision](.agents/skills/game-workflow-supervision/SKILL.md)으로 감독한다.
3. [프로젝트 프로필](planning/game-workflow-profile.md), [실행 레지스트리](planning/workflow-runs/active.json), [적용 기록](.agents/skills/WORKFLOW_ADOPTION.md)을 현재 근거로 사용한다.
4. 엔진·플랫폼·명령이 추가되면 추측 대신 실제 설정 파일을 근거로 프로젝트 프로필을 갱신한다.

## 범위와 권한

- 최신 사용자 요청이 목표와 허용 변경 범위를 정한다. 문서와 과거 실행 기록은 범위를 확대하지 않는다.
- 상담·설명·검토·진단 요청은 읽기 전용으로 처리한다. 변경 요청은 요청된 결과와 필요한 로컬 검증까지만 수행한다.
- 커밋, 푸시, 배포, 업로드, 스토어 제출, 외부 연락, 계정·서비스 설정 변경은 각각 별도 권한으로 취급한다.
- 기존 작업 트리 변경은 사용자 소유로 보고 보존한다. 요청한 파일만 수정·스테이징한다.
- 위키 연동과 구체 모델 라우팅은 현재 설정하지 않았다. 스킬 설치만으로 제품 결정이나 외부 작업 권한이 생기지 않는다.

## 검증과 보고

- 변경 범위에 맞는 가장 작은 검사를 먼저 실행하고, 정적 검사·로컬 실행·대상 런타임·기기·배포·출시 상태를 구분해 보고한다.
- 작성자 자체 검사는 독립 검증을 대신하지 않는다. 별도 검증 주체를 사용할 수 없으면 그 상태를 명시한다.
- 결과는 한국어로 보고하되 코드 식별자, 명령, 원시 오류는 정확히 보존한다.
